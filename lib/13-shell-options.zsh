# Shell behaviour.

setopt AUTOCD             # typing a directory name alone cd's into it
setopt NOBEEP             # no terminal bell on error
setopt NUMERIC_GLOB_SORT  # sort file10 after file9, not after file1
