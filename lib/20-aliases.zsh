# Personal aliases. Empty for now (none were set on the source machine) —
# add yours here so they travel to every machine this repo is installed on.
#
# Examples:
# alias zshconfig="$EDITOR $DOTFILES_DIR/zshrc"
# alias ll="ls -lah"
